#Primeiro: instala o pipupgrade (atualizador de packages)

pip install pipupgrade

#Segundo: utiliza o pipupgrade, atualizando todos os packages, inclusive instalando dependências importantes.

pipupgrade --latest --yes
